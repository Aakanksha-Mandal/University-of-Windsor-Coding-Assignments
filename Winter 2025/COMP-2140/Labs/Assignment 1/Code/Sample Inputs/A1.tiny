INT f2(INT x, INT y ) 
 BEGIN 
    z := x*x - y*y;
    RETURN z; 
 END 
 INT MAIN F1() 
 BEGIN
    INT x;
    INT y;
    READ(x, "A41.input"); READ(y, "A42.input");
    INT z;
    z := f2(x,y) + f2(y,x);
    WRITE (z, "file name.output"); 
 END
